using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoorTeleport : MonoBehaviour
{
    /*public Transform location;
    public GameObject player;
    public InputActionReference interactAction;
    private UnityEngine.XR.Interaction.Toolkit.Interactors.XRRayInteractor rayInteractor;

    void OnButtonPress()
    {
        RaycastHit hit;
        if (rayInteractor != null && hit.collider.CompareTag("Teleport") && rayInteractor.TryGetCurrent3DRaycastHit(out RaycastHit hit))
        {
            player.transform.position = location.transform.position;
            player.transform.rotation = location.transform.rotation;
            player.transform.localScale = location.transform.localScale;
        }
    }*/
}
